import React from 'react';

export function QuestionViewRange({ questionSchema, choice, submitAnswer }) {
  return null;
}

export function AnswerViewRange({ questionSchema, choice }) {
  return null;
}
